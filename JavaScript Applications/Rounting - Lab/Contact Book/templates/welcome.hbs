<section id="welcome">
    <div class="container centered">
        {{> header}}

        {{#if isAuth}}
            {{> contactPage}}
           {{else}}
            {{> loginForm}}
        {{/if}}

        {{> footer}}
    </div>
</section>